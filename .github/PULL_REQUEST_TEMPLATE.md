## 改动说明 · What & why


## 检查清单 · Checklist

- [ ] 只编辑了 `content/**/*.md`（没有手改生成物 `index.html`）
- [ ] 本地跑过 `npm run build`，并提交了重新生成的 `index.html`
- [ ] 新条目填了必填字段（`title`/`category`/`source`/`deployment`/`role`/`desc`/`capability`/`limitation`/`invocation`）
- [ ] `source` ∈ native/open/cloud，`deployment` ∈ on-device/cloud/hybrid；`related` 指向真实存在的 slug
- [ ] 信息可核对，并用 `updated` 标注了核对时间
